#include <iostream>

using namespace std;

int n, m;
int a[1005][1005];
int d[1005];

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m;
    for(int i=0;i<n;i++) {
        for(int j=0;j<m;j++) {
            cin >> a[i][j];
        }
    }

    d[1]
}